# Branch and Tag Strategy

This document describes the branch layout and tag naming convention for MonoTune.

---

## Active Branches

| Branch | Purpose |
|--------|---------|
| `dev` | Main development branch. All feature work targets `dev`. Default integration branch. |
| `release/0.10.x` | Maintenance branch for the 0.10.x release line. Bug-fix cherry-picks only. |
| `feature/*` | Short-lived feature branches. Merged into `dev` via pull request when ready. |

All other branches from the OuterTune upstream (`canary`, `fdroid`, `lite`, `weblate-staging`,
`feature/library-manager`, `release/0.6.x`–`release/0.9.x`) have been deleted.
Run `.github/workflows/cleanup-repo.yml` (once, manually) to apply this deletion on the remote.

---

## Tag Naming Convention

```
v{major}.{minor}.{patch}
v{major}.{minor}.{patch}-a{N}    (alpha)
v{major}.{minor}.{patch}-b{N}    (beta)
v{major}.{minor}.{patch}-rc{N}   (release candidate)
```

**Examples:** `v0.10.2-b1`, `v0.10.2-rc1`, `v0.10.2`

### Tag Normalization

The following tags use non-standard naming inherited from OuterTune and will be renamed
by `cleanup-repo.yml`:

| Old tag | New tag |
|---------|---------|
| `pre_rel-0.10.0-rc1` | `v0.10.0-rc1` |
| `pre_rel-0.10.1-b1` | `v0.10.1-b1` |
| `pre_rel-0.10.2-b1` | `v0.10.2-b1` |
| `v0.8.0-final` | `v0.8.0` |
| `v0.9.0-final` | `v0.9.0` |

All historical `v0.6.x`–`v0.9.x` tags and `v0.10.0`, `v0.10.1` are kept unchanged.

---

## Migration Status

| Area | Status |
|------|--------|
| App rebrand (MonoTune name, namespace) | ✅ Done |
| Local audio scanning / playback — **UI default** | ✅ `LocalLibraryEnableKey` now defaults to `false`; new installs do not enable scanning |
| Local audio scanning / playback — **code removal** | ✅ Done — scanner classes, settings fragments, `LocalPlayerSettings`, storage permissions, `DirectoryTree`, `FolderScreen`, OOBE page removed |
| Local audio scanning / playback — `utils/scanners/` package | ✅ Done — `UriFileUtils.kt` relocated to `utils/`; `utils/scanners/` package eliminated |
| DB migration: MonoTune tables (v20→v21) | ✅ Done — `monochrome_track_match`, `manual_correction`, `ytm_scrobble_queue` added |
| DB migration: remove `isLocal` (v21→v22) | ✅ Migration spec defined; schema 22.json auto-generated by KSP on first build |
| MonoTune DAO layer | ✅ Done — `MonoTuneDao` in `db/daos/`; wired into `DatabaseDao` |
| YouTube Music search, playlists, likes, history | ✅ Retained |
| Monochrome integration layer (`MonochromeClient`, DB entities) | ✅ Stub in place |
| Monochrome authentication (`MonochromeAuthRepository`, login UI, Hilt DI) | ✅ Done — `MonochromeLoginScreen`, `MonochromeAuthRepository` (DataStore-backed), `MonochromeModule` Hilt binding, session restore in `App.kt` |
| Monochrome API connection | ✅ Done — real HTTP calls wired; `serverUrl` configurable per session |
| Monochrome search integration | ✅ Done — YTM results filtered to Monochrome-available tracks; badge + TIDAL cover in search UI |
| Monochrome playback | ✅ Done — `MusicService` resolves stream URL via Monochrome first, falls back to YouTube |
| Lyrics integration (Plan C) | ✅ Done — `MonochromeLyricsProvider` added; Monochrome/LRCLib-first with provider fallback chain |
| YTM scrobble drain worker | 🔲 Pending — queue table and DAO are ready; `WorkManager` worker not yet written |
| YTM → Monochrome playlist wiring | 🔲 Pending — playlist detail unresolved-tracks section not yet built |

---

## Key Changed Files

| File | Change |
|------|--------|
| `app/src/main/java/com/nohimazin/monotune/db/daos/MonoTuneDao.kt` | New — CRUD for all three MonoTune DB tables |
| `app/src/main/java/com/nohimazin/monotune/db/DatabaseDao.kt` | Extended to include `MonoTuneDao` |
| `app/src/main/java/com/nohimazin/monotune/utils/UriFileUtils.kt` | Relocated from `utils/scanners/` |
| `app/src/main/java/com/nohimazin/monotune/db/entities/PlaylistEntity.kt` | Corrected `isLocal` comment |
| `app/src/main/java/com/nohimazin/monotune/db/MusicDatabase.kt` | DB version 22; migration 21→22 spec |
| `app/schemas/.../21.json` | Schema for v21 (committed) |
| `app/schemas/.../22.json` | ⚠️ Not yet committed — generated by KSP on first build |
| `app/src/main/java/com/nohimazin/monotune/monochrome/MonochromeAuthRepository.kt` | New — DataStore-backed auth repository |
| `app/src/main/java/com/nohimazin/monotune/di/MonochromeModule.kt` | New — Hilt DI bindings for Monochrome |
| `app/src/main/java/com/nohimazin/monotune/ui/screens/MonochromeLoginScreen.kt` | New — Material3 login/account screen |
| `app/src/main/java/com/nohimazin/monotune/viewmodels/MonochromeLoginViewModel.kt` | New — login/session ViewModel |

---

## Remaining Gaps

### 1 — Schema 22.json

Run `./gradlew :app:kspDebugKotlin` (or a full debug build) and commit the generated
`app/schemas/com.nohimazin.monotune.db.InternalDatabase/22.json`.

### 2 — YTM scrobble drain worker

`ytm_scrobble_queue` table and `MonoTuneDao` are in place.  A `WorkManager`
`CoroutineWorker` is still needed to:

- Drain pending rows via `getPendingScrobbles()`
- POST each event to the YTM "mark as played" endpoint
- Call `markScrobbled(id)` on success, or discard after configurable retry limit
- Schedule itself with an exponential-back-off `PeriodicWorkRequest`

### 3 — YTM → Monochrome playlist wiring

Search is now wired.  Remaining work:

- Add the "unresolved tracks" section to the playlist detail screen
- Persist user-supplied track overrides via `MonoTuneDao.upsertManualCorrection()`
- Build a UI flow for the user to manually pick a Monochrome track for an unresolved YTM track

---

## Next Steps

1. Run a debug build and commit the generated `22.json` schema file.
2. Implement the YTM scrobble `WorkManager` drain worker (see gap 2 above).
3. Build the YTM → Monochrome playlist unresolved-tracks UI (see gap 3 above).
